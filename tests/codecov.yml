codecov:
  notify:
  require_ci_to_pass: yes

coverage:
  range: "80...100"
  round: down
  precision: 0
  status:
    project:

status:
  project: yes
  patch: yes
  changes: no

comment:
  layout: “diff, files”
  behavior: default
  require_changes: no
